These files are the record of schema changes applied to the Laivy Hart (haivyhart) Supabase database, in numerical order.

If a file and the live schema ever disagree, the live schema wins — these are a record, not the source of truth.
